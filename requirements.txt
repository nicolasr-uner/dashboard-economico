streamlit>=1.35
pandas>=2.0
plotly>=5.18
httpx>=0.27
beautifulsoup4>=4.12
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.0
python-dotenv>=1.0.0
feedparser>=6.0.10
anthropic>=0.40
statsmodels>=0.14
openpyxl>=3.1
# Dependencias opcionales/pesadas — importadas con try/except
# playwright>=1.44     # solo si se usa scraping dinámico
# celery>=5.3.6        # solo si se usa Celery con Redis
# redis>=5.0.1         # solo si se usa Redis
# chromadb>=0.5.0      # solo si se usa ChromaDB vectorial
# pydataxm>=0.3        # alternativa a HTTP directo para XM
